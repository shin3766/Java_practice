package me.yoosup.JavaJungSuk.lambda.md0528;

import java.util.stream.Stream;

/**
 * 스트림 만들기 - 배열 하이라이트
 * 1. 배열을 소스로 하는 스트림 생성 메서드 배우기
 */
public class stream_sec18 {

    // 1. 가변인자로 된 배열을 매개변수로 받을 때
//    Stream<T> Stream.of(T...values)

    // 2. 배열을 매개변수로 받을때
    // Stream<T> Stream.of(T[])

    // 3.
    // Stream<T> Arrays.stream(T[])

    // 4. 배열에 범위를 정할때
    // Stream<T> Arrays.stream(T[], int start, end)
}
